## Issues Identified

- Fee estimation is still not working.

## Unused Files

The following files are present in the project but are currently unused:

- `lib/arc.ts`
- `lib/mongodb.ts`
- `api/estimate/route.ts`

## Unused Dependency

- `mongodb` npm package